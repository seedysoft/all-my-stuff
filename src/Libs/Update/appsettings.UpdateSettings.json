{
  "$schema": "https://json.schemastore.org/appsettings.json",
  "UpdateSettings": {
    //  "CronExpression": "10 3 * * *",  // At 03:10
    "CronExpression": "*/2 * * * *", // At every 2nd minute.
    "ProcessStartInfoFileName": "/bin/bash",
    // "ProcessStartInfoArguments": "-c /usr/bin/systemd-run --collect --no-ask-password --on-active=60 --same-dir --user sudo update.sh {0}"
    "ProcessStartInfoArguments": "-c /mnt/st/Seedysoft/linux-arm64/update.sh /mnt/st/Seedysoft/linux-arm64/{0}"
  }
}