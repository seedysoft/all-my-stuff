namespace Seedysoft.Libs.MapRazorClassLibrary.MapModels.Base;

/// <summary>
/// A set of methods shared between event-powered classes (like <see cref="Map"/> and <see cref="UILayers. Marker"/>).
/// Generally, events allow you to execute some function when something happens with an object (e.g. the user clicks on the map, causing the map to fire '<c>click</c>' event).
/// </summary>
internal abstract class Evented { }
