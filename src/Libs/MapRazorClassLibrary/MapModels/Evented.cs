namespace Seedysoft.Libs.MapRazorClassLibrary.MapModels;

/// <summary>
/// A set of methods shared between event-powered classes (like <see cref="Map"/> and <see cref="Marker"/>).
/// Generally, events allow you to execute some function when something happens with an object (e.g. the user clicks on the map, causing the map to fire '<c>click</c>' event).
/// </summary>
public record class Evented { }
