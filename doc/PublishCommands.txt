# FIRST UPGRADE NUGET PACKAGES (PS>):
dotnet package update

# THEN SET VARIABLES (PS>):

$PublishDirectory="$env:OneDriveCommercial\_Syncthing\Seedysoft\linux-arm64\"

$PublishDirectory="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\publish\linux-arm64\"
$PublishDirectory="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\publish\win-arm64\"
$PublishDirectory="$env:USERPROFILE\_prog\seedysoft\Test\linux-x64\"

$Configuration="Debug"
$Configuration="Release"

# <RuntimeIdentifiers>linux-arm64;linux-x64;win-x64</RuntimeIdentifiers>
# <RuntimeIdentifiers>linux-arm64;win-x64</RuntimeIdentifiers>
# <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>

# $Runtime="linux-x64"
# $Runtime="win-x64"
$Runtime="linux-arm64"

$version=(Get-Date).ToUniversalTime().ToString("yy.Mdd.Hmm.ss")

export DOTNET_ENVIRONMENT=Development

$Configuration="Debug"
$ProjectOrSolution="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\src\Libs\GasStationPrices.ConsoleApp\Seedysoft.Libs.GasStationPrices.ConsoleApp.csproj"
dotnet publish $ProjectOrSolution -p:PublishDir=$PublishDirectory -c $Configuration -r $Runtime -p:AssemblyVersion=$version --sc ;

$Configuration="Release"
$ProjectOrSolution="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\src\Outbox\ConsoleApp\Seedysoft.Outbox.ConsoleApp.csproj"
dotnet publish $ProjectOrSolution -p:PublishDir=$PublishDirectory -c $Configuration -r $Runtime -p:AssemblyVersion=$version --sc ;
$ProjectOrSolution="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\src\Pvpc\ConsoleApp\Seedysoft.Pvpc.ConsoleApp.csproj"
dotnet publish $ProjectOrSolution -p:PublishDir=$PublishDirectory -c $Configuration -r $Runtime -p:AssemblyVersion=$version --sc ;
$ProjectOrSolution="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\src\WebComparer\ConsoleApp\Seedysoft.WebComparer.ConsoleApp.csproj"
dotnet publish $ProjectOrSolution -p:PublishDir=$PublishDirectory -c $Configuration -r $Runtime -p:AssemblyVersion=$version --sc ;
$ProjectOrSolution="$env:USERPROFILE\_prog\seedysoft\all-my-stuff\src\BlazorWebApp\Server\Seedysoft.BlazorWebApp.Server.csproj"
dotnet publish $ProjectOrSolution -p:PublishDir=$PublishDirectory -c $Configuration -r $Runtime -p:BlazorCacheBootResources=false -p:PublishReadyToRun -p:PublishReadyToRunShowWarnings=true -p:AssemblyVersion=$version --sc ;

# Publish all projects from Raspberrypi4 ($>):

# version="$(date --utc +'%y.%-m%d.%-H%M.%-S')"

# dotnet publish /mnt/wd/_prog/seedysoft/all-my-stuff/Seedysoft.Test.sln -p:PublishDir=/mnt/st/Seedysoft/linux-arm64/ -c $Configuration -r linux-arm64 -p:BlazorCacheBootResources=false -p:AssemblyVersion=$version --sc ;
# cd /mnt/st/Seedysoft/linux-arm64 ;

# dotnet publish /mnt/wd/_prog/seedysoft/all-my-stuff/Seedysoft.Test.sln -p:PublishDir=/mnt/st/Seedysoft/win-x64/ -c $Configuration -r win-x64 -p:BlazorCacheBootResources=false -p:AssemblyVersion=$version --sc ;

# Para ver errores podemos añadir --verbosity

#   Explicación de las opciones:
#        -c  | --configuration
#        -p  | --property
#        -r  | --runtime
#       --sc | --self-contained
#        -v  | --verbosity <LEVEL> # Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic]. The default is minimal.
